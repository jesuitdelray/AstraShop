export interface Product {
    id: number
    name: string
    is_new?: boolean
    images: string[]
    price: number
    description?: string
    attributes?: any
    // description
    // attributes
    // parrent id
}
